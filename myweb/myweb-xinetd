service http
{
       socket_type             = stream
       wait                    = no
       user                    = root
       server                  = /usr/local/bin/scr-wrap 
       server_args	       = arg1
}

